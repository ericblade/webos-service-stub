{
  "name": "webos-service-stub",
  "version": "2.0.2",
  "description": "Simulator and Test module for webOS node services",
  "main": "lib/ServiceStub.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --recursive --exit",
    "coverage": "./node_modules/.bin/nyc --reporter=html ./node_modules/.bin/mocha --recursive --exit"
  },
  "author": "Eric Blade",
  "license": "MIT",
  "dependencies": {
    "fast-deep-equal": "^2.0.1",
    "unique-filename": "^1.1.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0"
  }
}
